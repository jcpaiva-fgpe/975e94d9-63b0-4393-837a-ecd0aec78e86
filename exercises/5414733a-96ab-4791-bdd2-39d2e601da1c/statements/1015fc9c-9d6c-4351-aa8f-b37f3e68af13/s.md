You can add new properties to JavaScript objects the same way you modify them:
```
let myObject = {};
myObject.isCool = true;
console.log(myObject.isCool);

>>> true
```
\
Try to add a new property _max_ with a value of 10.