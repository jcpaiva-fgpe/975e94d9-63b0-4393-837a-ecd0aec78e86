function main() {
    for(let i = 0; i < 7; i++) {
        let line = "*";
        for(let x = 0; x < i; x++) {
            line = line + "*";
            
        }
        console.log(line);
    }
}