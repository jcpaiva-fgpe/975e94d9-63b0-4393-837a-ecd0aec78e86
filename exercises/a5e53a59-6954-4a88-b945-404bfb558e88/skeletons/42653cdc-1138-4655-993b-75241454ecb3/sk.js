// Don't touch anything, just add the console.log() somewhere inside! :)
function main() {
    var first = 3;
    var second = 2;

    var sum = first + second;
    console.log("Sum: " + sum);
}
