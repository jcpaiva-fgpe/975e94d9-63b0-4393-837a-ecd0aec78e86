function main() {
    let myDog = {
    // Only change code below this line
        legs: 4,
        tails: 1,
        friends: ["cats"]

    // Only change code above this line
    };

    console.log(Object.keys(myDog));
}